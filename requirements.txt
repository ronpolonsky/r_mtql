ogbench == 1.1.0
ml_collections
matplotlib
moviepy
wandb
wandb_osh
d4rl
gymnasium == 0.29.1  # We can't use gymnasium >= 1.0.0 because it breaks d4rl.
gym == 0.23.1  # For d4rl.
shimmy[gym-v21,gym-v26]  # For d4rl.
Cython < 3  # For d4rl.

# take expo and pip install the following
